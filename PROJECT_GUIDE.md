# دليل Engaz Clinic Demo

آخر مراجعة: 2026-08-12

## 1. الهدف

عرض تجريبي Static لواجهة إدارة عيادة. كل الأسماء والأرقام داخله بيانات صناعية، ولا يمثل نظام مرضى حقيقيًا.

## 2. النسخة الرسمية والتشغيل

- المستودع: `SheedoZ/Engaz_Clinic_Demo`
- الفرع: `main`
- المسار المحلي: `D:\Projects\Engaz-Clinic-Demo`
- الإنتاج: `https://clinic.engaz.premiumpower-eg.com/` عبر GitHub Pages.

## 3. تركيب الملفات

- `index.html`: الواجهة والبيانات التجريبية والمنطق الخاص بالعيادة.
- `v2-core.js`: وظائف الواجهة المشتركة والتنظيف والعرض.
- `v2-theme.css`: تصميم V2.
- `CNAME`, `.nojekyll`: إعداد Pages والنطاق.
- `.github/workflows/validate.yml`: فحص syntax والروابط والخصوصية ومنع الشبكة والتخزين.
- `README.md` و`PROJECT_GUIDE.md`: التعليمات.

## 4. طريقة التعديل

أنشئ فرعًا من `main`، وعدّل الملفات الثلاثة الخاصة بالواجهة فقط. حافظ على البيانات الصناعية، وعلى عبارة عدم إدخال بيانات مرضى حقيقية، وعلى `noindex,nofollow`. افتح Pull Request ولا تدمج قبل نجاح `Static safety`.

## 5. اختبار صغير

```powershell
node --check v2-core.js
git diff --check
```

افتح `index.html` وجرّب بوابة الدخول التجريبية، الفلاتر، نافذة الموعد، الحفظ الوهمي وReset. لا تدخل بيانات حقيقية.

## 6. النشر والرجوع

الدمج في `main` يعيد نشر GitHub Pages تلقائيًا. راجع النطاق بعد نجاح Pages. للرجوع نفّذ `git revert` للـcommit المسبب للمشكلة ثم ادمج التراجع.

## 7. حدود الأمان

لا API ولا قاعدة بيانات ولا تخزين دائم ولا إرسال رسائل. لا تضف بيانات مرضى أو تحليلات أو اتصالات خارجية أو أسرار. بوابة البداية للعرض وليست حماية أمنية.
